{
  "name": "oneagent-public-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run check && astro build && npm run validate",
    "check": "astro check",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "preview": "astro preview",
    "validate": "node scripts/validate-build.mjs",
    "test:all": "npm test && npm run build && npm run test:e2e"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.1.4"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@playwright/test": "1.62.0",
    "axe-core": "4.12.1",
    "jsdom": "30.0.0",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
