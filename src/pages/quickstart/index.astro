---
import BaseLayout from "../../layouts/BaseLayout.astro";
---
<BaseLayout title="快速开始 — BootAgent" description="从下载到首次 Agent 配置成功的 BootAgent 快速开始指南。">
  <header class="page-hero">
    <div class="shell page-hero-grid">
      <div>
        <nav class="breadcrumb" aria-label="面包屑"><a href="">首页</a> / 快速开始</nav>
        <h1 class="page-title">六步完成第一次激活</h1>
      </div>
      <div class="page-hero-aside"><strong>目标</strong>完成一个 Agent、一个 Provider 和一个实际模型协议的配置，并知道 BootAgent 写了什么。</div>
    </div>
  </header>
  <section class="section-compact">
    <div class="shell content-grid reveal">
      <article class="prose">
        <div class="notice notice-info">
          <span class="notice-mark">i</span><div><strong>macOS 版已签名并通过 Apple 公证</strong><p>自 v0.7.0 起，macOS 产物带 Developer ID 签名并经公证，校验 SHA-256 后直接打开即可。Windows 版的 Authenticode 签名尚未完成，SmartScreen 仍会提示一次，这也是渠道仍叫技术预览版的原因之一。</p></div>
        </div>
        <h2 id="download">1. 下载并校验</h2>
        <p>从下载中心选择与你的平台和架构匹配的产物。运行前计算 SHA-256，并与下载页显示的值逐字核对。</p>
        <h2 id="launch">2. 启动 BootAgent</h2>
        <p>macOS 打开 DMG 后把应用拖入「应用程序」，Windows 运行安装器，Linux 给下载的文件加可执行权限。界面在本机回环地址上提供服务，不暴露到局域网或公网；关闭应用后本地服务随之退出。</p>
        {/* The four-step Gatekeeper bypass that lived here promised its own
            removal: "签名与公证完成后，上面四步都不再需要，届时这份指南也会撤下。"
            v0.7.0 delivered the signature, so the walkthrough is gone — teaching
            an allowance would now only serve outdated builds, and the safer move
            for those is an upgrade, not an exception. The heading and anchor stay
            because the download page and external links still point here. */}
        <h3 id="macos-gatekeeper">macOS 首次打开</h3>
        <p>自 v0.7.0 起，macOS 产物带 Developer ID 签名，并已通过 Apple 公证。首次打开只有 macOS 对下载文件的常规确认，不再出现“无法验证开发者”的拦截，也不需要在系统设置里放行任何东西。</p>
        <p>如果你打开的是 v0.6.x 或更早的历史预览版，Gatekeeper 仍会拦截它——那些包没有签名。不要为旧版本添加放行例外：<a href="downloads/">升级到当前版本</a>，拦截随签名一起消失。</p>
        <p>这条边界不变：BootAgent 从不要求你执行 <code>spctl --master-disable</code>、关闭 Gatekeeper 或降低系统整体安全设置。</p>
        <h2 id="agent">3. 选择 Agent</h2>
        <p>选择你真正要使用的工具。标记为“官方安装引导”的 Agent 仍可进入流程，安装与配置由官方渠道完成，BootAgent 如实标注这一点。</p>
        <h2 id="provider">4. 连接自己的 Provider</h2>
        <p>选择 Provider 或自定义兼容端点，并使用你自己的 API Key。BootAgent 不提供共享 Key，不建立统一模型网关，也不代收模型费用。</p>
        <h2 id="probe">5. 测试真实协议</h2>
        <p>模型列表可达不等于 Agent 一定可用。BootAgent 会按 Agent 使用的协议测试连接：OpenAI Chat Completions、Anthropic Messages 或 OpenAI Responses。</p>
        <h2 id="review">6. 复核写入与下一步</h2>
        <p>完成页会展示每个 Agent 的结果、配置位置与启动方式。配置前会创建备份；错误不会被静默推迟到 Agent 的第一次请求。</p>
        <div class="notice notice-info">
          <span class="notice-mark">i</span><div><strong>Key 保存在哪里？</strong><p>Key 只写入目标 Agent 工作所需的本地配置或 BootAgent 环境文件，不写入 BootAgent profile、日志、截图或遥测。</p></div>
        </div>
        <h2 id="troubleshooting">遇到问题</h2>
        <ul>
          <li>缺少 Agent：按页面给出的官方来源安装，不使用不明镜像脚本。</li>
          <li>Key 被拒绝：回到 Provider 控制台检查权限与余额，不要把 Key 发到聊天或 Issue。</li>
          <li>协议不兼容：换用明确支持该 Agent 协议的模型，而不是只验证 <code>/v1/models</code>。</li>
          <li>下载校验不一致：不要运行文件，保留文件名、下载地址与实际 SHA-256。</li>
          <li>macOS 提示“已损坏，无法打开”：先核对 SHA-256。如果打开的是 v0.6.x 或更早的旧版本，这通常是旧包的公证拦截，<a href="downloads/">升级到当前签名版本</a>即可；当前版本哈希一致仍报损坏，就重新下载一次。</li>
        </ul>
        <p><a class="button button-primary" href="downloads/">前往下载中心</a></p>
      </article>
      <nav class="side-index" aria-label="本页目录">
        <a href="#download">1. 下载并校验</a><a href="#launch">2. 启动</a><a href="#macos-gatekeeper">macOS 首次打开</a><a href="#agent">3. 选择 Agent</a><a href="#provider">4. 连接 Provider</a><a href="#probe">5. 测试协议</a><a href="#review">6. 复核结果</a><a href="#troubleshooting">遇到问题</a>
      </nav>
    </div>
  </section>
</BaseLayout>
