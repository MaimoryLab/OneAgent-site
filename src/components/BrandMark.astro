---
interface Props {
  size?: number;
}
const { size = 28 } = Astro.props;
const source = `${import.meta.env.BASE_URL}images/brand/bootagent-logo-mark-256.png`;
---
<img src={source} alt="" width={size} height={size} aria-hidden="true" decoding="async" draggable="false" />
